#include <stdio.h>

int main() {
    int toplam = 0;

    for (int i = 5; i <= 1000; i += 5) {
        toplam += i;
    }

    printf("1 ile 1000 arasındaki 5'in katlarının toplamı: %d", sum);

    return 0;
}
